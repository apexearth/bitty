class Branch {
    constructor({
                    left, right, angle
                }) {
        this.left  = left
        this.right = right
        this.angle = angle
    }

    draw(seconds, graphics) {
        
    }
}

module.exports = Branch